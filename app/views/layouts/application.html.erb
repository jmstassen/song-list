<!DOCTYPE html>
<html>
  <head>
    <title>SongList</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="wrapper">
      <div class="nav">
        <% if logged_in? %>
        
            <%= link_to "Create new setlist", new_setlist_path %> | 
            <%= link_to "Create new song", new_song_path %> | 
            <%= link_to "My songs", user_songs_path(current_user) %> | 
            <%= link_to "My setlists", user_setlists_path(current_user) %> | 
            <%= link_to "All public songs", songs_path %> | 
            <%= link_to "Settings", edit_user_path(current_user) %> | 
            <%= link_to "Log out", logout_path, method: :delete %> 
            <br><br>
        <% else %>     
            
            <%= link_to "Sign up", signup_path %> | 
            <%= link_to "Log in", login_path %> | 
            <%= link_to "Log in with Google", '/auth/google_oauth2', method: :post %> 
            <br><br>
        <% end %>
      </div>
      <div>
        <% if flash[:message] %>
          <br>
          <%= flash[:message] %>
          <br>
        <% end %>
      </div>
      <%= yield %>
      <footer class="branding">
        <small>&copy; 2021 <strong>Song List</strong></small>
      </footer>
    </div>
  </body>
</html>
